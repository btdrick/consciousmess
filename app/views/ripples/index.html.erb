<p id="notice"><%= notice %></p>

  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav h5">
          <li class="nav-item">
            <a class="nav-link" href="/ripples">Newest</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/ripples">Next 10 Ripples</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/ripples">Previous 10 Ripples</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/ripples">Oldest</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/ripples/new">New Ripple</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <table class="table table-bordered table-striped">
    <thead class="thead-light">
      <tr>
        <th>Author</th>
        <th>Message</th>
        <th colspan="2">Posted</th>
      </tr>
    </thead>

    <tbody>
      <% @ripples.order(created_at: :desc).each do |ripple| %>
        <tr>
          <td><%= websitelink(ripple) %></td>
          <td><%= ripple.message %></td>
          <td><%= ripple.created_at %></td>
          <td><%= link_to 'Show', ripple %></td>
          <td><%= link_to 'Edit', edit_ripple_path(ripple) %></td>
          <td><%= link_to 'Destroy', ripple, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
